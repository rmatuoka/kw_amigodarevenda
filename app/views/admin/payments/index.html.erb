<% title "Formas de Pagamento" %>
<h1>Módulo de Formas de Pagamento</h1><br />
<p>Visualize abaixo a lista das formas de pagamento registrados no sistema.</p><br />

<table class="tablesorter">
  <thead>
  <tr>
    <th>Descricao</th>
    <th>Tipo</th>
    <th>Valor</th>
	<th colspan="3" align="center">Ações</th>
  </tr>
  </thead> 
  <tbody>
  	 <% for payment in @payments %>
	    <tr>
	      <td><%= payment.descricao %></td>
	      <td><%= payment.tipo %></td>
	      <td><%= payment.valor %></td>
	      <td><%= link_to "Show", [:admin, payment] %></td>
	      <td><%= link_to "Edit", edit_admin_payment_path(payment) %></td>
	      <td><%= link_to "Destroy", [:admin, payment], :confirm => 'Are you sure?', :method => :delete %></td>
	    </tr>
	  <% end %>
  </tbody>
</table>
<br />
<p><%= link_to "Cadastrar nova forma de pagamento", new_admin_payment_path %></p>
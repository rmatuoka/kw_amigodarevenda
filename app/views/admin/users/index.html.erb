<%" Administração de Usuários" %>

<h1>Módulo de Usuários</h1><br />
<p>Verifique abaixo a lista dos usuários cadastrados no sistema.</p><br />

<table cellspacing="0" class="tablesorter">
  <thead> 
  <tr>
    <th width="200px">Usuário</th>
	<th width="200px">Email</th>
	<th colspan="3" style="width:20%;">Ações</th>
  </tr>
  </thead> 
  <tbody>
  <% for user in @users %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email%></td>
      <td><%= link_to "Visualizar", admin_user_path(user) %></td>
      <td><%= link_to "Editar", edit_admin_user_path(user) %></td>
      <td><%= link_to "Excluir", admin_user_path(user), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<p><%= link_to "New User", new_admin_user_path %></p>